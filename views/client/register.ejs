<%- include('./layouts/head',
{title: 'Home Page'}
) %>
<%- include('./layouts/header',
) %>
<div class="register-area">
    <section class="banner-area">
        <div class="container">
            <div class="row">
                <div class="contact-form form-area">
                    <h2 class="panel-title">Register</h2>
                    <div class="">
                        <% if(hasErrors) { %>
                            <div class="alert alert-danger">
                                <% messages.forEach(function(message){ %>
                                    <span><%=  message %></span>
                                <% })%>
                            </div>
                        <% } %>
                        <form id="register-form" role="form" action="/user/register" method="post">
                            <input type="file" name="img" style="display: none">
                            <div class="left">
                                <input name="firstname" id="firstname" value="<%= dataForm.firstname %>" type="text" placeholder="First name">
                            </div>
                            <div class="right">
                                <input type="text" name="lastname" id="lastname" value="<%= dataForm.lastname %>" class="" placeholder="Last Name">
                            </div>
                            <div class="row">
                                <div class="col-3">
                                    <div style="width: 50px" class="default-select" id="default-select">
                                        <select name="gender" value="<%= dataForm.gender %>" placeholder="gender">
                                            <option value="male">Male</option>
                                            <option value="famale">Female</option>
                                            <option value="other">Other</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-5">
                                    <input type="date" name="dob" value="<%= dataForm.dob %>" style="width: 300px" placeholder="Birthday">
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-6">
                                    <input style="width: 300px; margin-right: 20px" type="text" name="numbercmnd" value="<%= dataForm.numbercmnd %>"
                                           class="" placeholder="Number identity">
                                </div>
                                <div class="col-6">
                                    <div class="input-image">
                                        <label><i class="fa fa-image"></i> Input Image
                                            <input type="file" name="img" value="<%= dataForm.img %>" style="display: none">
                                        </label>
                                    </div>
                                </div>
                            </div>
                            <input type="text" name="address" value="<%= dataForm.address %>"
                                   class="" placeholder="Address">
                            <input type="email" name="email" id="email" value="<%= dataForm.email %>" class="" placeholder="Email Address">
                            <div class="left">
                                <input type="password" name="password" id="password" class="" placeholder="Password">

                            </div>
                            <div class="right">
                                <input type="password" name="password_confirmation" id="password_confirmation" class="" placeholder="Confirm Password">
                            </div>
                            <button type="submit" class="template-btn">Register</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
<%- include('./layouts/footer',
) %>
